name: Bazel test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: ${{ matrix.settings.name }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        settings:
          - {
              name: "bazel debug test",
              flags: "-c dbg",
          }
          - {
              name: "bazel optimized test",
              flags: "-c opt",
          }
          - {
              name: "bazel address sanitizer test",
              flags: "--config=asan",
          }
          - {
              name: "bazel undefined behaviour sanitizer test",
              flags: "--config=ubsan",
          }
          - {
              name: "bazel thread sanitizer test",
              flags: "--config=tsan",
          }
    steps:
      - uses: actions/checkout@v3
      - uses: bazelbuild/setup-bazelisk@v2
      - name: Mount bazel cache # Optional
        uses: actions/cache@v3
        with:
          path: "~/.cache/bazel"
          key: bazel
      - run: bazel test --action_env=BAZEL_CXXOPTS="-std=c++20" ${{matrix.settings.flags}} //...
